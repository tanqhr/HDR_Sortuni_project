spring:
  jpa:
    open-in-view: false
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: false
        show_sql: false
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never
      platform: h2
  mvc:
    hiddenmethod:
      filter:
        enabled: true

  mail:
    host: localhost
    port: 1025
    username: "test@example.com"
    password: "password"

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        api:
          to:

message:
  api:
    baseUrl: "http://localhost:8081"

google:
  maps:
    api-key:

whether:
  api-key:





